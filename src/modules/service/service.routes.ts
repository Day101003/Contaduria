import { Routes } from '@angular/router';
import { ServicesPageComponent } from './pages/service-page.component';

export const SERVICES_ROUTES: Routes = [
  { path: '', component: ServicesPageComponent }
];
